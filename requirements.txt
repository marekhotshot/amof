# AMOF Orchestrator dependencies
# Core agent functionality — LLM providers
anthropic>=0.40.0
boto3>=1.34.0
botocore>=1.34.0
openai>=1.50.0
pydantic>=2.7.0

# Linting (used by orchestrator post-edit hook and ReadLints tool)
ruff>=0.8.0

# YAML (agent config, manifests)
PyYAML>=6.0

# Vector Database Memory (pysqlite3-binary provides sqlite3 >= 3.35 for ChromaDB on older systems)
pysqlite3-binary>=0.5.0
chromadb>=0.4.22

# API Server
fastapi>=0.100.0
uvicorn>=0.20.0
requests>=2.28.0
pytest>=8.3.5
